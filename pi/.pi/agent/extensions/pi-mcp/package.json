{
  "name": "pi-mcp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "MCP client for pi, ported from opencode's MCP implementation (proxy tool mode, lazy startup, OAuth)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"test/**/*.test.ts\""
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "@earendil-works/pi-ai": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
